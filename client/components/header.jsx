const React = require('react')

const Header = React.createClass({
  render: function () {
    return (
      <header>
        <h1><a href='/'>Revisit</a></h1>
      </header>
    )
  }
})

module.exports = Header
